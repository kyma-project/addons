{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "additionalData": {
            "type": "string",
            "title": "Additional data",
            "description": "Example attribute which has no function."
        },
        "onlyNumbersString": {
            "title": "RegExp data",
            "type": "string",
            "pattern": "^\\d*$",
            "description": "Testing field for RegExp pattern. Accepts only numbers."
        },
        "planName": {
            "type": "string",
            "title": "Plan name",
            "description": "The 'planName' value in the ConfigMap resource."
        }
    },
    "required": ["additionalData"]
}
