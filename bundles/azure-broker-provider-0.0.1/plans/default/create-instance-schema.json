{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "minimumStability": {
      "type": "string",
      "oneOf": [
        {
          "enum": ["EXPERIMENTAL"],
          "title": "Experimental"
        },
        {
          "enum": ["PREVIEW"],
          "title": "Preview"
        },
        {
          "enum": ["STABLE"],
          "title": "Stable"
        }
      ],
      "default": "PREVIEW",
      "title": "Minimum services stability",
      "description": "Minimum stability required for a module's services and plans to be listed in the broker's catalog. For production, use \"STABLE\" only."
    }
  },
  "required": ["minimumStability"]
}
